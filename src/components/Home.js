import React from "react";

const Home = () => (
    <div>
        <h1>Flashcards!</h1>
        <h2>Add questions and answers! and leave comments in the blog section!</h2>

    </div>
);

export default Home;